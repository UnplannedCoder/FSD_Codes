#include<stdio.h> // Preprocessor directive to include standard input and output header file  
int main(){  // Main function where the execution begins 
    printf("Hello World:\n"); 
    return 0;  //Returns 0 indicates successful execution of the program 
    //C language is a case sensitive language, so 'main' is different from 'Main' or 'MAIN'
    //';' is used to terminate statements in C 
}